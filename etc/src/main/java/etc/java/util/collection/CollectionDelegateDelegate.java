package etc.java.util.collection;

public class CollectionDelegateDelegate<E> extends _CollectionDelegateDelegate<E> {

	public CollectionDelegateDelegate(CollectionDelegate<E> delegate) {
		super(delegate);
	}
}